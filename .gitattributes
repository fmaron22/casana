# Normaliza saltos de línea a LF en el repo (evita ruido CRLF en Windows).
* text=auto eol=lf

# Binarios: sin conversión.
*.png binary
*.jpg binary
*.pdf binary
*.ai binary
*.woff binary
*.woff2 binary
