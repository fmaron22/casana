{
  "name": "casana",
  "private": true,
  "version": "0.1.0",
  "description": "Casana — plataforma para formalizar el empleo del hogar en México.",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "test": "node --test \"libs/**/*.test.mjs\" \"apps/**/*.test.mjs\"",
    "db:dev": "node scripts/dev-db.mjs",
    "db:check": "node scripts/check-db.mjs"
  },
  "devDependencies": {
    "embedded-postgres": "^18.4.0-beta.17"
  }
}
